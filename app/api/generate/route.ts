import { NextRequest, NextResponse } from "next/server";

type Platform = "小红书" | "抖音" | string;

function buildPrompt(keyword: string, platform: Platform): string {
  const isDouyin = platform === "抖音";

  const persona = isDouyin
    ? "你是一名百万播放的抖音口播文案博主，写作风格犀利、抓人、节奏感极强。"
    : "你是一名真实的小红书博主，写作风格真诚、有故事感、像在和闺蜜聊天。";

  const styleRules = isDouyin
    ? `【文案风格要求 - 抖音】
- 前 3 秒必须有钩子，第一句话直接抓住观众
- 全部使用短句，单句不超过 15 字
- 每段不超过 2 句话，节奏紧凑
- 大量制造反差和冲突，引发好奇
- 结尾必须引导关注，例如点赞、收藏、关注我`
    : `【文案风格要求 - 小红书】
- 开篇先制造痛点，让读者有"这说的就是我"的感觉
- 加入大量第一人称经历（我/我的/我用过），增强真实感
- 全文使用口语化表达，像发微信聊天一样自然
- 严禁 AI 腔，不要出现"作为一个 AI"、"首先、其次、最后"等套话
- 多用 emoji 表情增强情绪
- 结尾必须引导评论互动，例如"你们觉得呢？""评论区告诉我"`;

  return `${persona}

你必须严格遵循"专业爆款创作八步法"，按顺序输出【用户画像】【核心痛点】【传播爆点】【爆款结构】【5个爆款标题】【推荐标题】【原创文案】七个模块，缺一不可。

用户关键词：${keyword}

==========================================
第一步：分析关键词本身
==========================================
- 这个关键词的本质是什么
- 它属于哪一类话题（功效 / 情感 / 经验 / 测评 / 生活方式 ...）
- 这个话题天然带有什么张力或冲突

==========================================
第二步：分析关键词对应的目标用户
==========================================
- 性别、年龄、城市、收入水平
- 身份标签（学生 / 上班族 / 宝妈 / 健身党 / 数码爱好者 ...）
- 典型生活场景
- 核心心理需求

==========================================
第三步：分析目标用户最关心的 5 个痛点
==========================================
每个痛点必须满足：
- 真实存在的具体痛点，不是空话
- 让人看完想立刻点进来看
- 越扎心越好

==========================================
第四步：提炼最容易传播的爆点
==========================================
- 这个话题里"最容易让用户转发的核心钩子"是什么
- 找出 1 个最有传播力的情绪点（扎心 / 爽 / 燃 / 上头）

==========================================
第五步：选择最合适的爆款结构（关键步骤）
==========================================
从以下 5 种结构中**只选 1 种**最适合当前关键词的：

结构 A — 反常识型
   公式：你以为…… → 其实…… → 真相是……
   适用：推翻主流认知，颠覆用户既有想法

结构 B — 踩坑避雷型
   公式：我以为…… → 结果…… → 后来发现……
   适用：亲历失败，揭露行业潜规则

结构 C — 逆袭成长型
   公式：曾经…… → 转折…… → 现在……
   适用：从低谷爬出来，强调时间线和变化

结构 D — 悬念揭秘型
   公式：一个现象 → 隐藏原因 → 揭秘真相
   适用：现象级话题、爆款解读、深度揭秘

结构 E — 情绪共鸣型
   公式：用户痛苦 → 放大情绪 → 提供解决方案
   适用：扎心话题、情感宣泄、给方案

必须基于"关键词 + 用户画像 + 痛点 + 爆点"综合判断，并在【爆款结构】模块中输出：结构名称 / 选择原因 / 内容框架。

==========================================
第六步：生成 5 个高点击率爆款标题
==========================================
要求：
- 每个标题不超过 20 字
- 5 个标题必须**题材不同**，必须覆盖以下 5 种钩子：
  1) 反常识（颠覆认知 / 推翻常识）
  2) 焦虑（直击痛点 / 不行动就会怎样）
  3) 省钱（白嫖 / 薅羊毛 / 平替）
  4) 逆袭（素人翻身 / 普通人逆袭）
  5) 悬念（不剧透 / 一看就停不下来）
- 每个标题后面用括号注明该标题对应的钩子类型

==========================================
第七步：选择最佳标题
==========================================
从 5 个标题里选出 1 个作为【推荐标题】，并写一句话说明推荐理由。

==========================================
第八步：按照【爆款结构】生成 1 篇原创文案
==========================================
- 必须严格套用第五步选定的结构（反常识/踩坑避雷/逆袭成长/悬念揭秘/情绪共鸣）
- ${isDouyin ? "口播脚本" : "小红书种草文"}风格
- 强钩子开头：前 3 句话必须死死抓住注意力
- 有故事感：要有时间、地点、人物、转折
- 有情绪波动：要有痛点 → 挣扎 → 转折 → 释然 / 上头
- 有具体细节：数字、价格、场景、动作，越细越真实
- 有反转：至少 1 次认知反转
- 有互动引导：结尾必须引导评论或关注

【反 AI 味硬约束】（必须遵守，违者整篇作废）
- 严禁出现"我悟了""姐妹们""其实""其实不然""其实真相是""其实很多人不知道""首先 / 其次 / 最后 / 总之""作为一个""今天给大家分享""家人们""宝子们"等 AI / 自媒体模板高频词
- 严禁"空对空"说教：禁止"要学会……""我们应该……""一定要记住……"这类鸡汤式祈使句
- 必须用具体场景代替抽象表达：
   · 错："要学会合理规划时间" → 对："昨晚 11 点我还在改第 3 版方案"
   · 错："这款产品性价比很高" → 对："89 块买的吹风机，风量比 199 的戴森还猛"
- 必须用真实口语："踩雷""翻车""上头""破防""真香""破大防""绷不住了"等活人词随便用
- 必须有画面感：写到动作、温度、声音、表情、表情包一样的细节
- 必须有反转：至少 1 次认知反转，且反转要"反常识但不反逻辑"
- 必须有情绪波动：用户读得到你的"得意 / 后悔 / 上头 / 崩溃"

${styleRules}

==========================================
【输出格式】严格使用以下结构
==========================================
【用户画像】
...

【核心痛点】
1. ...
2. ...
3. ...
4. ...
5. ...

【传播爆点】
- ...

【爆款结构】
结构名称：...（反常识型 / 踩坑避雷型 / 逆袭成长型 / 悬念揭秘型 / 情绪共鸣型）
选择原因：...
内容框架：...（用 3-5 个短句说明这篇文案将如何套用该结构，每句对应一个段落）

【5个爆款标题】
1. ...（反常识）
2. ...（焦虑）
3. ...（省钱）
4. ...（逆袭）
5. ...（悬念）

【推荐标题】
...
推荐理由：...

【原创文案】
...

要求：
- 严格使用以上 7 个小标题，不要新增或删减
- 全文用中文输出
- 不要输出任何解释、寒暄、Markdown 代码块`;
}

export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    const { keyword, platform } = body;

    if (!keyword) {
      return NextResponse.json(
        { success: false, error: "缺少 keyword 参数" },
        { status: 400 }
      );
    }

    console.log("当前平台：", platform);

    const apiKey = process.env.DEEPSEEK_API_KEY;
    if (!apiKey) {
      return NextResponse.json(
        { success: false, error: "未配置 DEEPSEEK_API_KEY" },
        { status: 500 }
      );
    }

    const prompt = buildPrompt(keyword, platform);

    const response = await fetch("https://api.deepseek.com/chat/completions", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${apiKey}`,
      },
      body: JSON.stringify({
        model: "deepseek-chat",
        messages: [
          { role: "system", content: "你是一名资深自媒体爆款文案专家。" },
          { role: "user", content: prompt },
        ],
        temperature: 0.7,
      }),
    });

    if (!response.ok) {
      const errorText = await response.text();
      return NextResponse.json(
        { success: false, error: `DeepSeek API 错误: ${errorText}` },
        { status: response.status }
      );
    }

    const data = await response.json();
    const content = data.choices?.[0]?.message?.content ?? "";

    return NextResponse.json({
      success: true,
      content,
    });
  } catch (error) {
    const message = error instanceof Error ? error.message : "未知错误";
    return NextResponse.json(
      { success: false, error: `服务器错误: ${message}` },
      { status: 500 }
    );
  }
}
